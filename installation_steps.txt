# For enable offline install of python library
# Download the dependency in another pc which have internet
mkdir wheelhouse && pip download -r requirements.txt -d wheelhouse

# For installation at offline computer
# move requrments.txt to "wheelhouse"
mv ./requirements.txt ./wheelhouse/  # i dont know if this step is required or not
# install the dependency
pip install -r wheelhouse/requirements.txt --no-index --find-links wheelhouse

